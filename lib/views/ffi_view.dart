import 'package:flutter/material.dart';

class FfiView extends StatelessWidget {
  const FfiView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FFI and ffigen demos'),
    );
  }
}
